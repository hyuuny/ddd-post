spring:
  output:
    ansi:
      enabled: always
  jpa:
    properties:
      hibernate:
        default_batch_fetch_siz: 1000
        format_sql: true


logging:
  level:
    com.setge.dddpost: debug
    org.springframework: info

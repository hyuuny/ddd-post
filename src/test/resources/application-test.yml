spring:
  output:
    ansi:
      enabled: always
  jpa:
    properties:
      hibernate:
        format_sql:true
        #default_batch_fetch_siz: 1000


logging:
  level:
    com.setge.dddpost: debug
    org.springframework: info
